name: pytest

on:
  push:

jobs:
  pytest:
    strategy:
      fail-fast: false
      matrix:
        python-version: [3.8]
        # poetry-version: [1.5.1]
        # os: [ubuntu-20.04, macos-latest, windows-latest]
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Python Setup Rye
        uses:  eifinger/setup-rye@v1
      - name: Install packages
        run: /home/runner/work/_temp/rye_temp_home/shims/rye sync
      - name: Run pytest
        run: /home/runner/work/_temp/rye_temp_home/shims/rye run pytest tests
